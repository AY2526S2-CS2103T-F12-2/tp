package seedu.address.logic.commands;

/**
 * The flags used by <code>EditCommand</code>. -r corresponds to reset, -a corresponds to append.
 */
public enum EditFlag {
    APPEND,
    RESET,
    NONE
}
